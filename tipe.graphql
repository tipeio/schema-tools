type Hero @type {
  "This title will be the main text on the her section"
  title: String!
    @ui(
      component: "SINGLE_LINE",
      name: "Title for the hero"
    )
    @validations(
      maxlength: 120
    )
  mainImg: Asset
  description: String
}

type HomePage implements Page
@page(url: "https://mydemopage.com") {
  pageInfo: PageInfo!
  heroSection: Hero!
}

type Social {
  twitter: Url
  linkedin: Url
}

type Author @type {
  firstName: String!
  lastName: String!
  socialInfo: Social!
  bio: String!
}

type BlogPost @type {
  title: String!
  postContent: String!
  author: Author!
}
